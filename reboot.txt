# 开机时可以在这里添加自定义启动命令
# 例如：启动 OpenClaw 服务器

# 注意：OpenClaw 和数据目录位于 runner 用户下
# - OpenClaw 安装位置：/home/runner/.nvm/versions/node/v22.*/bin/openclaw
# - 数据目录：/home/runner/.openclaw
# - SSH 登录后切换到 runner 用户：su - runner

# OpenClaw 使用说明：
# 1. 如果已有配置（从备份恢复），直接启动：
#    su - runner -c 'openclaw'
# 2. 如果是首次使用，需要先配置：
#    su - runner -c 'openclaw onboard'
# 3. 安装为守护进程（开机自动启动）：
#    su - runner -c 'openclaw onboard --install-daemon'

# 自动启动示例（取消注释以使用）：
# su - runner -c 'cd ~/.openclaw && openclaw' &

